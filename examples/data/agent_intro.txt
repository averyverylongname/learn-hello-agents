Agent 是一种能够围绕目标进行推理、规划和行动的智能系统。

与普通聊天机器人相比，Agent 不只是生成文本回答，还可以调用外部工具、读取文件、搜索资料、执行代码或操作系统。

ReAct 是一种经典的 Agent 范式，它让大模型交替进行 Thought、Action 和 Observation，从而实现边思考边行动。

在工程实现中，一个 Agent 框架通常包括 LLM 调用层、消息管理、工具系统、记忆模块和检索模块。

RAG 可以看作是 Agent 获取外部知识的一种方式。Agent 可以通过检索工具查询文档，再基于检索结果生成回答。